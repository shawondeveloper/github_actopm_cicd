name: GITHUB CICD
on: push
jobs:
  build-node:
    runs-on: ubuntu-latest
    container: node:20
    steps:
      - run: node --version
      - run: npm --version
      - uses: actions/checkout@4
        with:
            fetch-depth: 0
      - run: npm install
      - run: node app.js
